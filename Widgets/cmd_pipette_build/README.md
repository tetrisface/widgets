# Pipette Build

Copy a blueprint of the unit/building under the mouse cursor, and set the current active command to building that blueprint (as long as one of your selected builders can build it). 

Default keybind is 'shift-q'.

If you copy on top of empty space, the previously successfully copied blueprint will be used again. 
